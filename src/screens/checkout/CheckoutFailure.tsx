import React from 'react';
import { View } from 'react-native';

const CheckoutFailure = () => {
  //checkout failure can be handled in this screen, but it is out of scope for this test
  return <View />;
};

export default CheckoutFailure;
